# Weekly In Tech edition workspace

Load the `weekly-in-tech-production` skill before using this scaffold.
Replace all example story facts, URLs, dates, assets, timings, titles, and hashes.
The bundled renderer is a reference implementation, not a content template to publish unchanged.
Never promote an artifact until source, voice, caption, decode, montage, and human-review gates pass.
